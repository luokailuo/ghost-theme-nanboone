{{!< default}}

<main class="site-main">
    <div class="inner-wide">
        <div class="post-wrap">
            {{#foreach posts}}
            {{> "post-card"}}
            {{/foreach}}
        </div>
        {{pagination}}
    </div>
</main>